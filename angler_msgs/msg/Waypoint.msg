# A goal pose for the UVMS to navigate to.

# The goal pose.
geometry_msgs/PoseStamped pose

# The desired acceleration when passing through the waypoint (m/s^2).
float64 acceleration

# The desired velocity when passing through the waypoint (m/s).
float64 velocity
